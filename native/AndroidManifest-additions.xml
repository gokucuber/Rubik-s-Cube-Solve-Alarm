<!--
  Paste these into android/app/src/main/AndroidManifest.xml after running
  `npx cap add android`. Capacitor generates the file, so it is not checked in.

  The permission block goes above <application>; the receiver goes inside it.
-->

<uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
<uses-permission android:name="android.permission.USE_EXACT_ALARM" />
<uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
<uses-permission android:name="android.permission.WAKE_LOCK" />
<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

<!-- Bluetooth, declared without location since nothing here needs a position. -->
<uses-permission android:name="android.permission.BLUETOOTH_SCAN"
    android:usesPermissionFlags="neverForLocation" />
<uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

<!-- Inside <application>: -->
<receiver
    android:name=".AlarmReceiver"
    android:exported="false"
    android:enabled="true" />

<!--
  Also set these on the existing <activity android:name=".MainActivity">:

      android:showWhenLocked="true"
      android:turnScreenOn="true"
      android:launchMode="singleTask"
      android:excludeFromRecents="false"
-->
